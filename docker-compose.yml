version: "3.9"

services:
  backend:
    build: ./backend
    container_name: liber
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./frontend:/app/frontend:ro
    env_file: .env
